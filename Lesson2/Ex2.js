const browsers = [
    "Chrome",
    "Firefox",
    "Edge"
];

// 1. Add Safari
browsers.push("Safari");

// 2. Remove the last browser
browsers.pop();

// 3. Print the final array
console.log(browsers);